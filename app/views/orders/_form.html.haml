= simple_form_for(@order) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :address
    = f.input :email
    = f.input :pay_type, collection: Order::PAYMENT_TYPES

  .form-actions
    = f.button :submit
