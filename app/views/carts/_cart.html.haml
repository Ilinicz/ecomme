-unless cart.line_items.empty?
  %h5 Cart
  %ul.list-group
    = render partial: 'line_items/line_item', collection: cart.line_items
  / - @cart.line_items.each do |item|
  /  %li.line-item
  /    = item.quantity
  /    \× 
  /    = link_to item.product.title, store_path(item.product)
  /    = number_to_currency(item.total_price)
  /    = button_to item, method: :delete, class:'btn btn-xs btn-danger' do
  /      .glyphicon.glyphicon-trash
  %h4= "Total: #{number_to_currency(cart.total_price)}"
  
  = button_to "Empty", cart, method: :delete, data:{confirm: 'Sure?'}, class: 'btn btn-danger btn-xs'